apiVersion: v1
kind: Service
metadata:
  namespace: unknown-namespace
  name: svc-unknown-name
  labels:
    app: unknown-app
    release: unknown-release
    group: unknown-group
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    - port: 1337
      targetPort: 1337
      protocol: TCP
      name: health
  selector:
    app: unknown-name
    release: unknown-release