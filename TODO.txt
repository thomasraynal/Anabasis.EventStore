use snapshot for subscribe to all
proper private setter for dto
handle projections

do we need a T for streamId =>
  public interface IEntity<TKey> : IHaveAStreamId
  {
    TKey EntityId { get; set; } 
  }
